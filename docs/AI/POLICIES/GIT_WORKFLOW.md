# Git Workflow

## After Kiro Pushes

On local machine:

```powershell
cd "D:\SaaSprojectService\Rasti chekFinal 10 tir"
git status
```

If clean:

```powershell
git pull
```

If not clean:

Do not pull.

Send `git status --short` to ChatGPT.

---

## Before Commit

Run related tests.

Review changed files.

Commit only focused work.
