# Multi-Tenant Rules

Company isolation is mandatory.

Every company-owned object must be accessed through company scope.

Public invoice links must only expose intended invoice information.

Admin/operator/technician/customer roles must not cross tenant boundaries.
